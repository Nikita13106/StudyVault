# Leave EMPTY in development — requests go through the Vite proxy (vite.config.js).
# In production, set this to your deployed API base URL, e.g.
# VITE_API_URL=https://studyvault-api.onrender.com
VITE_API_URL=
